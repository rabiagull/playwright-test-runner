{
  "name": "mytest-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @mytest/core -w @mytest/reporter -w @mytest/runner",
    "mytest:run": "mytest run"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "playwright": "^1.49.0",
    "typescript": "^5.6.0"
  }
}
